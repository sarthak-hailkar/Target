python
def login():
  print("Logged in")

login()


Do not use TOP in PostgreSQL use LIMIT keyword
Replace each TOP keyword with LIMIT
if query has iff() use CASE statement in PostgreSQL.
SQL Server - [database].[schema].[table]  ,  PostgreSQL-  database.schema.table formte
use ascii() instead of unicode().
use chr() instead of char().
CHARINDEX is not supported in PostgreSQL , use Position .
If + is used to concat string then use || in PostgreSQL .
use ATAN2() instead of ATN2() .
use CURRENT_TIMESTAMP instead of getdate();
use EXTRACT(MONTH FROM TIMESTAMP value) instead of MONTH() .
use EXTRACT(YEAR FROM TIMESTAMP value) instead of YEAR().
use EXTRACT(DAY from TIMESTAMP value) instead of DAY() .
Do not DECLARE variable with _ .
Do not use  AS $function$  for PostgreSQL   use  AS $$  .
FUNCTION in POSTGRES must end with END;$$  .
Do not use # in PostgreSQL in table name.
Do not use #temptable  in PostgreSQL use only temptable.
Create Temporary Table in PostgreSQL as CREATE TEMPORARY TABLE tablename  without # .
